# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# vite.config.ts sets outDir to `build`, so this is the real output folder.
# Vercel runs its own `npm run build`, so committing it only made every commit
# carry a churn of renamed hashed bundles and hid the actual source changes.
build
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
